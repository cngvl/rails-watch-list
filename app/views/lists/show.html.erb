<h1>Show html found at: app/views/lists/show.html.erb</h1>

<ul>
<% @list.movies.each do |movie| %>
<li><%= movie.title %></li>
<% end %>
</ul>

<%= link_to 'add movies to this list', new_list_bookmark_path(@list) %>
